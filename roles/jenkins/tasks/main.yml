- name: Create folders
  community.general.jenkins_job:
    config: "{{ lookup('file','templates/jenkins-folder.xml') }}"
    name: "{{ item }}"
    url: http://44.197.226.106:8080
    user: admin
    password: "{{jenkins_password}}"
  loop: "{{ folders }}"


- name: Create jobs in jenkins
  ansible.builtin.include_tasks: pipeline.yml
  loop: "{{ pipeline_jobs }}"
